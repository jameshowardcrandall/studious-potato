# Environment — never commit real credentials
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Dataverse solution zips — unpacked source under solutions/<Name>/ is the
# primary source of truth; the .zip is kept committed for easy distribution
# (direct download / pac solution import), so nothing excluded here.

# Power Apps downloaded artifacts
*.msapp.download

# Local editor state
.DS_Store
.vscode/settings.json
.claude/
.mcp.json

# PAC CLI
.pacx/
